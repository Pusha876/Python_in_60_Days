# This code calculates the length of the list and prints it out.
# However, the length of the list is always the same,
# so the output will always be the same.
# The length of the list should be calculated outside the loop.

mylist = ['a', 'b', 'c', 'd']
list_length = len(mylist)

for item in mylist:
    print(list_length)
