# This code will take the input from the user and capitalize the first letter
# in an infinite loop. The user will not be able to exit the loop.

name = input("Enter your name? ")

while True:
    print(name.capitalize())
